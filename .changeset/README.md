# Changesets

Run `pnpm changeset` for a user-facing package change. Choose the semantic
version bump and write the release-note summary. The release workflow converts
pending changesets into a version PR, then publishes only after that PR merges.
